<%= @results.count%>
<% articles.each do |article|%>
  <br>
  <%#= article.title %>
  <%# link to show article %>

  <%= link_to highlight(article.title, params[:title_search]) %>
  <%#= highlight(article.title, params[:title_search])%>
<% end %>
