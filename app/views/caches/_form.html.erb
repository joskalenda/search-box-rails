<%= form_with(model: cach) do |form| %>
  <% if cach.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(cach.errors.count, "error") %> prohibited this cach from being saved:</h2>

      <ul>
        <% cach.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
